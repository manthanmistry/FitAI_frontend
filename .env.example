# Points the frontend at the backend API.
# For local dev (vite dev server + backend running separately):
VITE_API_URL=http://localhost:8000/api

# For Docker Compose / production behind nginx reverse proxy, use:
# VITE_API_URL=/api
